<article><header class="page-heading"><a class="text-link back-link" href="<%= url_for('/archives/') %>">← All notes</a><span class="eyebrow"><time datetime="<%= date(page.date, 'YYYY-MM-DD') %>"><%= date(page.date, 'MMMM D, YYYY') %></time></span><h1><%= page.title %></h1></header><div class="prose article-prose"><%- page.content %></div></article>
